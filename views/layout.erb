<!DOCTYPE html>
<html>
<head>
  <title><%= @title ? "#{@title} - " : '' %>News Memory</title>
  <meta charset="utf-8">
  <%= (['news-memory', 'fancybox/jquery.fancybox' ] + @css_include).collect{|css| "<link href=\"/#{css}.css\" rel=\"stylesheet\" type=\"text/css\"/>"}.join("\n") %>
  <%= (['jquery', 'fancybox/fancybox', 'news-memory'] + @js_include).collect{|js| "<script src=\"/#{js}.js\" type=\"text/javascript\"></script>"}.join("\n") %>
 </head>
<body>

<%= yield %>

</body>
</html>